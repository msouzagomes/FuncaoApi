### GENERAL ###
server:
  port: 8081

spring:
  ### SLEUTH ###
  sleuth:
    baggage:
      remote-fields:
        - X-CCRED-TRANSACTIONAL-ID

### INTEGRATION ###
feign:
  client:
    config:
      default:
        loggerLevel: full

### CIRCUITBREAKER ###
resilience4j.circuitbreaker:
  instances:
    AutBankClient:
      registerHealthIndicator: true
      slidingWindowSize: 10
      permittedNumberOfCallsInHalfOpenState: 3
      slidingWindowType: COUNT_BASED #TIME_BASED
      minimumNumberOfCalls: 4
      waitDurationInOpenState: 10s
      failureRateThreshold: 50
      eventConsumerBufferSize: 10
      #recordFailurePredicate: io.github.robwin.exception.RecordFailurePredicate

resilience4j.retry:
  instances:
    AutBankClient:
      maxAttempts: 3
      waitDuration: 3s
      #enableExponentialBackoff: true
      #exponentialBackoffMultiplier: 2
      #retryExceptions:
      #   - org.springframework.web.client.HttpServerErrorException
      #  - java.io.IOException
      # ignoreExceptions:
      #  - io.github.robwin.exception.BusinessException

api:
  egress:
    host: ${API_FUNCAO-API_EGRESS_HOST:https://hml-pierlabs.calcard.com.br}
    consumer-key: ${API_FUNCAO-API_EGRESS_CONSUMER_KEY:123456}
  autbank:
    consumer-key: Basic c3VwZXJ2aXNvcjpzdXBlcnZpc29y
    abovws:
      username: SUPERVISOR
      rest:
        host: http://10.0.8.12:8080
        basePath: /ABOVWS/api
        factor: /fator
        createProposal: /propostai
        effectiveProposal: /proposta
        simulate: /simulacao
        operation: /operacao
        installment: /parcela
    abcvws:
      username: SUPERVISOR
      password: supervisor
      soap:
        urlService: http://10.0.8.12:8080/portal2CVWS/services/WS_Boleto
        namespace: http://boletos.dg.cv.autbank.com.br
    abibws:
      soap:
        urlService: http://10.0.8.12:8080/ABIBWS/services/IB_ServicesLT
        namespace: http://literal.autbank.com.br